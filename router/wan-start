read -p "What can I do for you? (add/del/upd/all)\n" opr
if [ $# -eq 0 ] || [ "$opr" == "all" ]; then
    chrt.sh del
    chrt.sh upd
    chrt.sh add
    exit
fi

if [ $# -ne 1 ]; then
    echo $0 add/del/upd/all
    exit
fi

chrt.sh $opr